// Deprecated: kill-v2 cleanup (May 2026). This was the AnalysisFlow component
// that orchestrated the /v2 alternative UI. /v2 was retired in favor of the
// main page at /. File kept as empty stub because GitHub MCP cannot delete
// files. Safe to physically remove with:
//   git rm src/components/flow/AnalysisFlow.tsx
export {};
