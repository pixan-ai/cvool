import type { Metadata } from "next";
import { pageMetadata } from "@/lib/metadata";
export const metadata: Metadata = pageMetadata("how", "C\u00f3mo funciona", "How it works", "Pipeline paso a paso, stack t\u00e9cnico, principios de dise\u00f1o y c\u00f3mo forkear cvool.", "Step-by-step pipeline, tech stack, design principles, and how to fork cvool.");
export { default } from "./page";
