import type { Metadata } from "next";
import { pageMetadata } from "@/lib/metadata";
export const metadata: Metadata = pageMetadata("security", "Seguridad", "Security", "C\u00f3mo protegemos tu CV: sin base de datos, sin cuentas, TLS, rate limiting, open source.", "How we protect your resume: no database, no accounts, TLS, rate limiting, open source.");
export { default } from "./page";
